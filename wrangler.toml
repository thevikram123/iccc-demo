# Cloudflare Workers config — used by wrangler CLI (optional)
# If deploying via the Cloudflare Dashboard or REST API, this file is not needed.
#
# To deploy with wrangler (if you have it installed via any means):
#   wrangler deploy
#   wrangler secret put GEMINI_API_KEY   ← paste your key when prompted

name = "iccc-gemini-proxy"
main = "worker.js"
compatibility_date = "2024-09-23"
